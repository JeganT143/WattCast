[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = ["slow: loads every committed model bundle"]

[tool.ruff]
line-length = 120
extend-exclude = ["notebooks", "env"]

[tool.ruff.lint]
select = ["F", "E9"]
