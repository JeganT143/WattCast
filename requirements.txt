# Runtime dependencies for the Streamlit app and the forecasting core.
# This is the file Streamlit Community Cloud installs. Pins are the versions
# the project was developed and tested with (Python 3.14).
# CPU-only torch wheel. --find-links (not --extra-index-url) keeps PyPI the
# source for every other package, which uv-based installers require.
--find-links https://download.pytorch.org/whl/torch/

streamlit==1.64.0
altair==6.3.0
pandas==3.0.6
numpy==2.5.3
scikit-learn==1.9.1
skops==0.15.0
torch==2.14.0+cpu
